import { View } from "react-native";
import styled from "styled-components";
import { RespScreenHeight } from "styles/sizes";

const PlusIconView = styled(View)`
  align-items: center;
  margin-top: ${RespScreenHeight(10)};
`;

export default PlusIconView;
